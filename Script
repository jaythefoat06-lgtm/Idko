-- [[ PEPSI-STYLE MENU: SAFE AUTO-STEAL ]] --
local UserInputService = game:GetService("UserInputService")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

-- UI SETUP
local SG = Instance.new("ScreenGui", game:GetService("CoreGui"))
local Frame = Instance.new("Frame", SG)
local Title = Instance.new("TextLabel", Frame)
local UIList = Instance.new("UIListLayout", Frame)

Frame.Size = UDim2.new(0, 220, 0, 320)
Frame.Position = UDim2.new(0.1, 0, 0.4, 0) -- Positioned to the side like the video
Frame.BackgroundColor3 = Color3.fromRGB(20, 20, 35)
Frame.BackgroundTransparency = 0.2
Frame.Active = true
Frame.Draggable = true

local Corner = Instance.new("UICorner", Frame)
Corner.CornerRadius = UDim.new(0, 10)

Title.Size = UDim2.new(1, 0, 0, 40)
Title.Text = "PEPSI V2 | SAFE"
Title.TextColor3 = Color3.fromRGB(180, 100, 255)
Title.BackgroundColor3 = Color3.fromRGB(30, 30, 50)
Title.Font = Enum.Font.GothamBold

UIList.Padding = UDim.new(0, 5)
UIList.HorizontalAlignment = Enum.HorizontalAlignment.Center

-- TOGGLE LOGIC
local AutoStealActive = false
local AutoStealRange = 20 -- Set to 20 studs for safety

-- FUNCTION: CREATE BUTTON
local function AddToggle(name, callback)
    local btn = Instance.new("TextButton", Frame)
    btn.Size = UDim2.new(0.9, 0, 0, 40)
    btn.Text = name
    btn.BackgroundColor3 = Color3.fromRGB(45, 45, 70)
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.GothamMedium
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)

    local active = false
    btn.MouseButton1Click:Connect(function()
        active = not active
        btn.BackgroundColor3 = active and Color3.fromRGB(120, 50, 255) or Color3.fromRGB(45, 45, 70)
        callback(active)
    end)
end

--- [[ THE AUTO-STEAL LOOP ]] ---
task.spawn(function()
    while task.wait(0.2) do -- Checks 5 times per second
        if AutoStealActive and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
            local myPos = LocalPlayer.Character.HumanoidRootPart.Position
            
            for _, obj in pairs(workspace:GetDescendants()) do
                if obj:IsA("ProximityPrompt") then
                    -- CALCULATION: Distance between you and the item
                    local itemPos = obj.Parent:IsA("BasePart") and obj.Parent.Position or obj.Parent:FindFirstChildWhichIsA("BasePart") and obj.Parent:FindFirstChildWhichIsA("BasePart").Position
                    
                    if itemPos then
                        local distance = (myPos - itemPos).Magnitude
                        
                        -- Only fire if you are within the safe range
                        if distance <= AutoStealRange then
                            fireproximityprompt(obj)
                        end
                    end
                end
            end
        end
    end
end)

--- [[ ADDING THE BUTTONS ]] ---

AddToggle("Auto-Steal (Safe)", function(state)
    AutoStealActive = state
end)

AddToggle("Speed (80)", function(state)
    local hum = LocalPlayer.Character:FindFirstChild("Humanoid")
    if hum then hum.WalkSpeed = state and 80 or 16 end
end)

AddToggle("Infinite Jump", function(state)
    _G.InfJump = state
    UserInputService.JumpRequest:Connect(function()
        if _G.InfJump then
            LocalPlayer.Character:FindFirstChildOfClass("Humanoid"):ChangeState("Jumping")
        end
    end)
end)

-- Toggle Menu Visibility (Right Control)
UserInputService.InputBegan:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.RightControl then
        SG.Enabled = not SG.Enabled
    end
end)
