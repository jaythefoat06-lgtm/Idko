--[[
    Steal a Brainrot - Main Script
    Version: 1.0.0
    Author: YOUR_USERNAME
]]

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer
local ChatService = ReplicatedStorage:FindFirstChild("DefaultChatSystemChatEvents")

-- Check if chat system exists
if not ChatService then
    warn("⚠️ Chat system not found! Some features may not work.")
end

-- Configuration
local CONFIG = {
    TOGGLE_KEY = Enum.KeyCode.RightShift,
    COMMAND = ";balloon",
    THEME = {
        PRIMARY = Color3.fromRGB(155, 89, 182),
        SECONDARY = Color3.fromRGB(30, 30, 40),
        BACKGROUND = Color3.fromRGB(20, 20, 30),
        TEXT = Color3.fromRGB(255, 255, 255),
        HOVER = Color3.fromRGB(80, 80, 90),
        SELECTED = Color3.fromRGB(155, 89, 182),
        DANGER = Color3.fromRGB(255, 50, 50)
    }
}

-- Wait for character
repeat wait() until LocalPlayer.Character

-- Create GUI
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "BrainrotStealer"
screenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")
screenGui.ResetOnSpawn = false

-- Main frame
local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 300, 0, 400)
mainFrame.Position = UDim2.new(0.5, -150, 0.5, -200)
mainFrame.BackgroundColor3 = CONFIG.THEME.SECONDARY
mainFrame.BackgroundTransparency = 0.1
mainFrame.BorderSizePixel = 0
mainFrame.Active = true
mainFrame.Draggable = true
mainFrame.Parent = screenGui

-- Rest of the GUI creation code (same as previous script)...
-- [Paste the full GUI creation code from the previous response here]
-- Make sure to replace color values with CONFIG.THEME references

print("✅ Steal a Brainrot loaded successfully!")
print("Press RightShift to toggle the GUI")
